{
  "name": "@ai-cleanup/core",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest"
  },
  "dependencies": {
    "@ai-cleanup/types": "workspace:*",
    "sharp": "^0.33.0",
    "pdf-parse": "^1.1.1",
    "image-hash": "^6.0.1"
  },
  "devDependencies": {
    "@types/pdf-parse": "^1.1.4",
    "typescript": "^5.3.2",
    "vitest": "^1.0.0"
  }
}

