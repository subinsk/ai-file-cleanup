# API Configuration
# The URL of the backend API server
# Use localhost instead of localhost to avoid IPv6 issues on Windows
VITE_API_URL=http://localhost:3001

# App Environment
VITE_APP_ENV=development

# Note: Variables prefixed with VITE_ are exposed to the renderer process
# Variables without VITE_ prefix are only available in the main process

