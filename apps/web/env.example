# API Configuration
# The URL of the backend API server (must start with NEXT_PUBLIC_ to be exposed to browser)
# Use localhost instead of localhost to avoid IPv6 issues on Windows
NEXT_PUBLIC_API_URL=http://localhost:3001

# App Environment
NEXT_PUBLIC_APP_ENV=development

# NextAuth Configuration (for production)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-key-here

