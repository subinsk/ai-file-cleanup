{
  "buildCommand": "cd ../.. && pnpm build:packages && cd apps/web && pnpm build",
  "installCommand": "cd ../.. && pnpm install --filter=@ai-cleanup/web... --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
