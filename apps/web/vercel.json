{
  "buildCommand": "cd ../.. && pnpm install --filter=@ai-cleanup/web... --ignore-scripts && cd packages/db && npx prisma generate --generator client && cd ../../apps/web && pnpm build",
  "outputDirectory": ".next"
}
